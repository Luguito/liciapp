import PropTypes from 'prop-types';
export declare const IconValues: {
    iconSize: number;
};
export declare const IconPropTypes: {
    /** OPTIONAL number, defines icon size */
    iconSize: PropTypes.Requireable<number>;
};
